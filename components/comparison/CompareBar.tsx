'use client'; import Link from 'next/link'; import {GitCompareArrows,X} from 'lucide-react'; import {useApp} from '../AppProviders'; import {parks} from '../../lib/data/parks'; export default function CompareBar(){const {compare,toggleCompare}=useApp();if(!compare.length)return null;return <div className="fixed inset-x-0 bottom-0 z-50 border-t bg-slate-950 text-white shadow-2xl"><div className="container-page flex min-h-16 items-center justify-between gap-4 py-2"><div className="flex items-center gap-2"><GitCompareArrows size={19}/><span className="font-semibold">{compare.length} Ferienpark{compare.length>1?'s':''} ausgewählt</span><div className="hidden gap-2 sm:flex">{compare.map(id=><button key={id} onClick={()=>toggleCompare(id)} className="rounded-full bg-white/10 px-2 py-1 text-xs">{parks.find(p=>p.id===id)?.name}<X size={12} className="ml-1 inline"/></button>)}</div></div><Link href="/vergleich/" className="rounded-xl bg-white px-4 py-2 text-sm font-bold text-slate-900">Vergleichen</Link></div></div>}
