import {facilityMap} from '../../lib/data/facilities'; import type {FacilityId} from '../../lib/types'; import {Check} from 'lucide-react'; export default function FacilityBadge({id}:{id:FacilityId}){return <span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2.5 py-1 text-xs font-medium text-slate-700"><Check size={13} className="text-emerald-600"/>{facilityMap[id]?.name}</span>}
