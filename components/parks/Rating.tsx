import {Star} from 'lucide-react'; export default function Rating({value,reviews}:{value:number;reviews:number}){return <div className="flex items-center gap-2"><span className="flex items-center gap-1 font-bold"><Star size={16} className="fill-amber-400 text-amber-400"/>{value.toFixed(1)}</span><span className="text-xs text-slate-500">({reviews.toLocaleString('de-DE')} Bewertungen)</span></div>}
